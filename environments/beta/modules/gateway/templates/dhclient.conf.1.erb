# Generated by puppet module dhcp
# Scenario : The dhcp server (for eth0 network) uses dhclient on eth1 to get an ip, 
# but wants to retain the DNS for the domain on eth0. 
<%
    nameserver = @zone['nameserver']
-%>
interface "<%= @name %>" {
    supersede domain-name-servers <%= nameserver['ip'] %>
}
